// Common dropdown option type
export interface Option {
  label: string;
  value: string;
}

// API response type for Organization
export interface Organization {
  id: string;
  name: string;
}

// API response type for Role
export interface Role {
  id: string;
  name: string;
}
