apiVersion: apps/v1
kind: Deployment
metadata:
  name: whilter-media-tools-frontend
  namespace: dev-charp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: whilter-media-tools-frontend
  template:
    metadata:
      labels:
        app: whilter-media-tools-frontend
    spec:
      containers:
        - name: whilter-media-tools-frontend
          image: 390844737706.dkr.ecr.ap-south-1.amazonaws.com/whilter-media-tools-frontend:1.1
          imagePullPolicy: Always
          env:
            - name: NODE_ENV
              value: "production"
          resources:
            requests:
              memory: "1Gi"
              cpu: "1"
            limits:
              memory: "1Gi"
              cpu: "1"
      