apiVersion: v1
kind: Service
metadata:
  name: whilter-media-tools-frontend-service
  namespace: dev-charp
spec:
  selector:
    app: whilter-media-tools-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP