apiVersion: v1
kind: Service
metadata:
  name: whilter-marketplace-frontend-service
  namespace: dev-charp
spec:
  selector:
    app: whilter-marketplace-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP